About
=====
Distribution package for camllib and fixpoint

Required
========
OCaml
Camlidl
C  libraries: 
  GMP (http://gmplib.org)
  MPFR (http://www.mpfr.org/)

To configure
============
- copy Makefile.config.model into Makefile.config
- set properly the flags

To compile and install
========================
make all # make all for required packages 
make install # make install 
